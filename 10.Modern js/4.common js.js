// //this will work in node js. not in browser
// //export
// export.addToCart = (product, quantity) => {
//     cart.push({ product, quantity });
//     console.log(`${quantity} ${product} added to the cart`);
//   };

// //import
// const {addToCart} = require('./1.shoppingcart')
